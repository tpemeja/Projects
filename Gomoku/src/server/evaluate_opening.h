
/* function which return an allocated table of col_move_t of an direct openingiven in parameters */
struct col_move_t * generate_direct_opening(size_t size, int i);

/* function which return an allocated table of col_move_t of an indirect openingiven in parameters */
struct col_move_t * generate_indirect_opening(size_t size, int i);
